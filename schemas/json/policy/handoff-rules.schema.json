{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://macp.dev/schemas/policy/handoff-rules.schema.json",
  "title": "Handoff Mode Governance Rules",
  "description": "JSON Schema for governance rules applied to macp.mode.handoff.v1 sessions. See RFC-MACP-0012 Section 4.5.",
  "type": "object",
  "properties": {
    "acceptance": {
      "type": "object",
      "properties": {
        "implicit_accept_timeout_ms": {
          "type": "integer",
          "minimum": 0,
          "default": 0,
          "description": "Auto-accept after this many milliseconds. 0 means no implicit accept.",
          "$comment": "The bound is inclusive on purpose and MUST NOT be tightened to exclusiveMinimum. 0 is the documented default and DISABLES implicit accept -- RFC-MACP-0010 Section 5.1 scopes the timer to the non-zero case explicitly -- so rejecting it would reject this field's own default. Audited under issue #116 piece 2."
        }
      },
      "patternProperties": {
        "^[_$]": true
      },
      "additionalProperties": false
    },
    "commitment": {
      "type": "object",
      "properties": {
        "authority": {
          "type": "string",
          "enum": [
            "initiator_only",
            "any_participant",
            "designated_role"
          ],
          "default": "initiator_only"
        },
        "designated_roles": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "patternProperties": {
        "^[_$]": true
      },
      "additionalProperties": false
    }
  },
  "allOf": [
    {
      "$comment": "Ported verbatim from decision-rules.schema.json, which has enforced this since it was written. `authority: \"designated_role\"` with no `designated_roles` declares that only designated roles may commit and then names none -- an unsatisfiable authority rule that either bricks the session or fails open depending on the runtime (issue #116). minItems 1 is part of the constraint: an empty array names no one either.",
      "if": {
        "properties": {
          "commitment": {
            "properties": {
              "authority": {
                "const": "designated_role"
              }
            },
            "required": [
              "authority"
            ]
          }
        },
        "required": [
          "commitment"
        ]
      },
      "then": {
        "properties": {
          "commitment": {
            "required": [
              "designated_roles"
            ],
            "properties": {
              "designated_roles": {
                "minItems": 1
              }
            }
          }
        }
      }
    }
  ],
  "patternProperties": {
    "^[_$]": true
  },
  "additionalProperties": false
}
