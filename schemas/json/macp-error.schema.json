{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://multiagentcoordinationprotocol.dev/schemas/macp-error.schema.json",
  "title": "MACP Error",
  "description": "Validates an MACPError message in JSON form. See RFC-MACP-0001.",
  "type": "object",
  "properties": {
    "code": {
      "type": "string",
      "minLength": 1,
      "description": "Machine-readable error code from the error code registry."
    },
    "message": {
      "type": "string",
      "description": "Human-readable error message. Text may vary across implementations."
    },
    "details": {
      "type": "object",
      "description": "Additional structured error details."
    }
  },
  "required": ["code"]
}
