{
  "_invalid_because": "`acceptance.implicit_accept_timeout` is not a declared property (RFC-MACP-0012 §4.5). The schema closes every object level with `additionalProperties: false`, so an unknown key is rejected instead of being silently ignored (issue #114). This is a dropped unit suffix, which would have left implicit accept DISABLED while the author believed they had set a 30s timer. Isolates the closure at the `acceptance` level: no `algorithm`/`type` key is set, so no conditional arm fires and nothing else can be the cause. The `_invalid_because` key you are reading is itself the proof the `^[_$]` annotation escape works -- it is an unknown key too, and it does NOT cause the rejection.",
  "acceptance": {
    "implicit_accept_timeout": 30000
  }
}
