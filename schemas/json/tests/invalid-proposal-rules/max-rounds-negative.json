{
  "_invalid_because": "counter_proposal.max_rounds has minimum 0; a negative round cap is meaningless (RFC-MACP-0008 §6). Isolates the minimum keyword. Note 0 IS legal and means no counter-proposals are permitted, so this fixture cannot use 0.",
  "counter_proposal": { "max_rounds": -1 }
}
