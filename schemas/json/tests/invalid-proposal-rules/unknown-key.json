{
  "_invalid_because": "`counter_proposal.max_round` is not a declared property (RFC-MACP-0012 §4.3). The schema closes every object level with `additionalProperties: false`, so an unknown key is rejected instead of being silently ignored (issue #114). This is a dropped plural, which would have left max_rounds at 0 -- unlimited -- while the author believed they had capped negotiation at 3. Isolates the closure at the `counter_proposal` level: no `algorithm`/`type` key is set, so no conditional arm fires and nothing else can be the cause. The `_invalid_because` key you are reading is itself the proof the `^[_$]` annotation escape works -- it is an unknown key too, and it does NOT cause the rejection.",
  "counter_proposal": {
    "max_round": 3
  }
}
